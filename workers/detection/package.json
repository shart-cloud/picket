{
  "name": "@picket/detection-worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./evaluator": {
      "types": "./dist/evaluator.d.ts",
      "import": "./dist/evaluator.js"
    }
  },
  "scripts": {
    "build": "pnpm --dir ../.. build:rules && pnpm --filter @picket/core build && pnpm --filter @picket/sigma-engine build && tsc -p tsconfig.json",
    "test": "pnpm --dir ../.. build:rules && pnpm --filter @picket/core build && pnpm --filter @picket/normalize build && pnpm --filter @picket/sigma-engine build && vitest run",
    "typecheck": "pnpm --dir ../.. build:rules && pnpm --filter @picket/core build && pnpm --filter @picket/normalize build && pnpm --filter @picket/sigma-engine build && tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@picket/core": "workspace:*",
    "@picket/normalize": "workspace:*",
    "@picket/sigma-engine": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.16.15"
  }
}
